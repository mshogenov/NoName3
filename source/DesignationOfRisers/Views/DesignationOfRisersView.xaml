<Window
    x:Class="DesignationOfRisers.Views.DesignationOfRisersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModel="clr-namespace:DesignationOfRisers.ViewModels" xmlns:cv="clr-namespace:DesignationOfRisers.Views.Converters"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModel:DesignationOfRisersViewModel}"
  
    WindowStartupLocation="CenterScreen"
    Title="Обозначение стояков"
    Height="450"
    Width="800">
    <Window.Resources>
        <cv:UnderscoreReplacerConverter x:Key="UnderscoreReplacerConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="auto" />
            <RowDefinition Height="2*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
            <ListBox ItemsSource="{Binding PipingSystemMdls}" Margin="10" >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                    <WrapPanel>
                        <CheckBox Content="{Binding Path=Name, Converter={StaticResource UnderscoreReplacerConverter}}" IsChecked="{Binding Path=IsChecked}" Width="300"/>
                        <ComboBox ItemsSource="{Binding Path=Marks}" SelectedValue="{Binding Path=SelectedMarkId, Mode=TwoWay}"
          SelectedValuePath="Id">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name, Converter={StaticResource UnderscoreReplacerConverter}}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </WrapPanel>
                </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        <ListBox Grid.Row="2" ItemsSource="{Binding Path=ViewsMdl}" Margin="10" IsEnabled="{Binding IsCheckedSelectedView}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Path=Name, Converter={StaticResource UnderscoreReplacerConverter}}" IsChecked="{Binding Path=IsCheked}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
            
        </ListBox>
        <StackPanel Grid.Row="1">
            <RadioButton Content="Активный вид" Margin="5" IsChecked="{Binding IsCheckedActiveView}"/>
            <RadioButton Content="Выбранные виды" Margin="5 0" IsChecked="{Binding IsCheckedSelectedView}"/>
        </StackPanel>
        <Button Grid.Row="3" Content="Запустить" Margin="5" Height="20" Command="{Binding Path=MarkRisersCommand}"  CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}"/>

    </Grid>
      
</Window>