<UserControl x:Class="UpdatingParameters.Views.SelectedCategoriesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:updatingParameters="clr-namespace:UpdatingParameters"
             xmlns:converters="clr-namespace:UpdatingParameters.Views.Converters"
             Height="600" Width="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Категории"
                   Margin="5"
                   FontWeight="Bold" />

        <Border Grid.Row="1"
                BorderBrush="Gray"
                BorderThickness="1"
                Margin="5">
            <ListBox ItemsSource="{Binding Categories}">
                <ListBox.Resources>
                    <Style x:Key="ItemBorderStyle" TargetType="Border">
                        <Setter Property="Padding" Value="5" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFE8E8E8" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <Style x:Key="ExpanderToggleStyle" TargetType="ToggleButton">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Width="16" Height="16" Background="Transparent">
                                        <Path x:Name="Arrow"
                                              Fill="Black"
                                              Data="M 0 0 L 8 0 L 4 4 Z"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Arrow" Property="Data"
                                                    Value="M 0 0 L 4 4 L 8 0 Z" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.Resources>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Border Style="{StaticResource ItemBorderStyle}">
                                <Grid>
                                    <!-- Определяем три колонки: для кнопки, отступа и содержимого -->
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="20"/> <!-- Для кнопки разворачивания -->
                                        <ColumnDefinition Width="5"/> <!-- Отступ -->
                                        <ColumnDefinition Width="*"/>  <!-- Для чекбокса и текста -->
                                    </Grid.ColumnDefinitions>

                                    <!-- Кнопка разворачивания в первой колонке -->
                                    <ToggleButton x:Name="Expander"
                                                  Grid.Column="0"
                                                  IsChecked="{Binding IsExpanded}"
                                                  Style="{StaticResource ExpanderToggleStyle}"
                                                  VerticalAlignment="Center">
                                        <ToggleButton.Visibility>
                                            <Binding Path="Children.Count">
                                                <Binding.Converter>
                                                    <converters:CountToVisibilityConverter/>
                                                </Binding.Converter>
                                            </Binding>
                                        </ToggleButton.Visibility>
                                    </ToggleButton>

                                    <!-- Чекбокс и текст в третьей колонке -->
                                    <StackPanel Grid.Column="2" 
                                                Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding IsChecked}" 
                                                  VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Name}" 
                                                   Margin="5,0,0,0" 
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <!-- Дочерние элементы с дополнительным отступом -->
                            <ItemsControl ItemsSource="{Binding Children}"
                                          Margin="40,0,0,0"> <!-- Увеличен отступ слева -->
                                <ItemsControl.Style>
                                    <Style TargetType="ItemsControl">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsExpanded}" Value="False">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ItemsControl.Style>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource ItemBorderStyle}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="20"/> <!-- Для выравнивания с родительским чекбоксом -->
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="1" 
                                                            Orientation="Horizontal">
                                                    <CheckBox IsChecked="{Binding IsChecked}" 
                                                              VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding Name}" 
                                                               Margin="5,0,0,0" 
                                                               VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="5">
            <Button Content="Выбрать все"
                    Width="100"
                    Margin="5"
                    Command="{Binding SelectAllCommand}" />
            <Button Content="Отменить выбор"
                    Width="100"
                    Margin="5"
                    Command="{Binding UnselectAllCommand}" />
        </StackPanel>
    </Grid>
</UserControl>