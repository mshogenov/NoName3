<UserControl x:Class="UpdatingParameters.Views.ParameterSelectorPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UpdatingParameters.Views"
             xmlns:controls="clr-namespace:NoNameApi.Views.Controls;assembly=NoNameAPI"
             mc:Ignorable="d"
           d:DesignHeight="300" d:DesignWidth="300">
    <Popup x:Name="Popup"
           Width="250"
         IsOpen="{Binding IsPopupOpen, RelativeSource={RelativeSource AncestorType=UserControl}}"
           PlacementTarget="{Binding CurrentPopupTarget, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <controls:SearchBox/>
            <Grid Grid.Row="1">
                <TabControl>
                    <TabItem Header="Параметры экземпляра">
                        <ListBox
                            ItemsSource="{Binding InstanceParameters, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            SelectedItem="{Binding SelectedParameter, RelativeSource={RelativeSource AncestorType=UserControl},
                                 Mode=TwoWay}"
                            Height="300">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Definition.Name}" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </TabItem>
                    <TabItem Header="Параметры типа">
                        <ListBox
                            ItemsSource="{Binding TypeParameters, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Height="300"
                            SelectedItem="{Binding SelectedParameter, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Definition.Name}" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </TabItem>
                </TabControl>
            </Grid>
            <DockPanel Grid.Row="2" HorizontalAlignment="Right">
                <Button Content="Закрыть"
                        Command="{Binding ClosePopupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            </DockPanel>
        </Grid>
    </Popup>
</UserControl>