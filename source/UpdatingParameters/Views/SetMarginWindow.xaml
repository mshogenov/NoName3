<UserControl x:Class="UpdatingParameters.Views.SetMarginWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:UpdatingParameters.Views"
             xmlns:viewmodel="clr-namespace:UpdatingParameters.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodel:SetMarginViewModel}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Content="Добавить категорию"
                Command="{Binding AddCategoryCommand}"
                VerticalAlignment="Top" />
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding MarginCategories }">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Category.Name}" />
                                    <TextBlock Text="{Binding Margin}" />
                                    <TextBlock Text="{Binding FromParameter.Definition.Name}" />
                                    <TextBlock Text="{Binding InParameter.Definition.Name}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>