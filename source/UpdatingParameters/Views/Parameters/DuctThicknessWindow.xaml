<views:BaseRevitWindow x:Class="UpdatingParameters.Views.Parameters.DuctThicknessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:UpdatingParameters.ViewModels.Parameters"
        xmlns:parameters="clr-namespace:UpdatingParameters.ViewModels.Parameters"
        xmlns:views="clr-namespace:NoNameApi.Views;assembly=NoNameAPI"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Параметры воздуховодов" Height="800" Width="1200"
        d:DataContext="{d:DesignInstance parameters:DuctThicknessViewModel }">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="pack://application:,,,/NoNameAPI;component/Views/Resources/Themes/ThemesResourceDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Style="{StaticResource Grid}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Панель инструментов -->
        <Border Style="{StaticResource BorderFolding}">
            <ToolBar Margin="5" Background="Transparent" BorderThickness="0">
                <ToolBar.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Margin" Value="3" />
                        <Setter Property="Padding" Value="8,5" />
                        <Setter Property="Background" Value="White" />
                        <Setter Property="BorderBrush" Value="#DDDDDD" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="3">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="{TemplateBinding Padding}" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F5F5F5" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#EEEEEE" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <Style TargetType="Separator">
                        <Setter Property="Margin" Value="5,0" />
                    </Style>
                </ToolBar.Resources>

                <!-- Кнопка Добавить -->
                <Button Command="{Binding AddCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE710;"
                                   FontFamily="Segoe MDL2 Assets"
                                   Foreground="#2196F3"
                                   FontSize="16"
                                   Margin="0,0,5,0" />
                        <TextBlock Text="Добавить" />
                    </StackPanel>
                    <Button.ToolTip>
                        <ToolTip>Добавить новую запись</ToolTip>
                    </Button.ToolTip>
                </Button>

                <!-- Кнопка Изменить -->
                <Button Command="{Binding UpdateCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE70F;"
                                   FontFamily="Segoe MDL2 Assets"
                                   Foreground="#4CAF50"
                                   FontSize="16"
                                   Margin="0,0,5,0" />
                        <TextBlock Text="Изменить" />
                    </StackPanel>
                    <Button.ToolTip>
                        <ToolTip>Изменить выбранную запись</ToolTip>
                    </Button.ToolTip>
                </Button>

                <!-- Кнопка Удалить -->
                <Button Command="{Binding DeleteCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE74D;"
                                   FontFamily="Segoe MDL2 Assets"
                                   Foreground="#F44336"
                                   FontSize="16"
                                   Margin="0,0,5,0" />
                        <TextBlock Text="Удалить" />
                    </StackPanel>
                    <Button.ToolTip>
                        <ToolTip>Удалить выбранную запись</ToolTip>
                    </Button.ToolTip>
                </Button>

                <Separator />

                <!-- Кнопка Копировать -->
                <Button Command="{Binding CopyCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE8C8;"
                                   FontFamily="Segoe MDL2 Assets"
                                   Foreground="#607D8B"
                                   FontSize="16"
                                   Margin="0,0,5,0" />
                        <TextBlock Text="Копировать" />
                    </StackPanel>
                    <Button.ToolTip>
                        <ToolTip>Создать копию выбранной записи</ToolTip>
                    </Button.ToolTip>
                </Button>

                <Separator />

                <!-- Кнопка Сохранить -->
                <Button Command="{Binding SaveCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE74E;"
                                   FontFamily="Segoe MDL2 Assets"
                                   Foreground="#FF9800"
                                   FontSize="16"
                                   Margin="0,0,5,0" />
                        <TextBlock Text="Сохранить" />
                    </StackPanel>
                    <Button.ToolTip>
                        <ToolTip>Сохранить данные</ToolTip>
                    </Button.ToolTip>
                </Button>
            </ToolBar>
        </Border>

        <!-- Таблица данных -->
        <Border Grid.Row="1"
                Margin="5"
                Background="White"
                BorderBrush="#DDDDDD"
                BorderThickness="1"
                CornerRadius="4">
            <DataGrid ItemsSource="{Binding DuctParameters}"
                      SelectedItem="{Binding SelectedParameter}"
                      Style="{StaticResource CleanDataGridStyle}"
                      ColumnHeaderStyle="{StaticResource CleanColumnHeaderStyle}"
                      CellStyle="{StaticResource CleanCellStyle}"
                      RowStyle="{StaticResource CleanRowStyle}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Материал"
                                        Binding="{Binding Material}"
                                        Width="*" />
                    <DataGridTextColumn Header="Сечение"
                                        Binding="{Binding Shape}"
                                        Width="*" />
                    <DataGridTextColumn Header="Внешняя изоляция"
                                        Binding="{Binding ExternalInsulation}"
                                        Width="*" />
                    <DataGridTextColumn Header="Внутренняя изоляция"
                                        Binding="{Binding InternalInsulation}"
                                        Width="*" />
                    <DataGridTextColumn Width="*" Binding="{Binding Size}">
                        <DataGridTextColumn.Header>
                            <TextBlock Text="Размер большей&#x0a;стороны (включительно)"
                                       TextWrapping="Wrap"
                                       TextAlignment="Center" />
                        </DataGridTextColumn.Header>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Толщина стенки"
                                        Binding="{Binding Thickness}"
                                        Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</views:BaseRevitWindow>