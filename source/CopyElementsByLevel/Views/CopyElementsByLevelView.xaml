<Window
    x:Class="CopyElementsByLevel.Views.CopyElementsByLevelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModel="clr-namespace:CopyElementsByLevel.ViewModels" xmlns:datacontexts="clr-namespace:CopyElementsByLevel.DataContexts" d:DataContext="{d:DesignInstance Type=datacontexts:CopyToLevelsContext}"
    mc:Ignorable="d"
   
   
    WindowStartupLocation="CenterScreen"
    Title="Копировать по уровням"
    Height="450"
    Width="800">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
       
            <TextBox Grid.Row="0" Grid.Column="0" 
             
             Text="{Binding Path=SerchValue, UpdateSourceTrigger=PropertyChanged}"/>
            <ListBox Grid.Row="1" Grid.Column="0" 
             ItemsSource="{Binding Path=FilteredLevelItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Margin="0" Content="{Binding Path=Name}" IsChecked="{Binding Path=IsSelected}"
                    Command="{Binding Path=DataContext.UpdateCountSelectedLevelItems, RelativeSource={RelativeSource AncestorType=ListBox}}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ListBox>
            
           
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0">
            <TextBlock VerticalAlignment="Center" Margin="10"
                 Text="Выбрано уровней:"/>
            <TextBlock VerticalAlignment="Center" Text="{Binding Path=CountSelectedLevelItems, Mode=OneWay}"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Center"
                Orientation="Horizontal">
            <Button Width="200" Command="{Binding Path=CopyElementsToLevelCommand}"
              Content="Принять"/>
            <Button Width="200"  Content="Отмена"/>
        </StackPanel>

    </Grid>
</Window>